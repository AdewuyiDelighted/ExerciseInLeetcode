package leetcodeSpecial;

public class MaximumProductOfLength {

    public static int maximumProductOfTheLength(String[] input) {
        String innerLoopWord = "";
        int count = input.length;
        for(int index = 0; index < input[count].length();index++){
            for(int indice = 0;indice < input.length;indice++){
                innerLoopWord = input[indice];
//                /if(input[count].charAt(index))
            }
        }
        return 0;
    }
}
