import java.util.Collections;

public class Combination {

    public static int[][] combination(int firstInput, int secondInput) {
        int firstLength = Math.max(firstInput,secondInput);
        int secondLength = Math.min(firstInput,secondInput);
        for(int index = 1; index < firstLength;index++){

        }
        return null;
    }
}
